# PULSE Edge UI styles

This package is the Edge-owned snapshot of the PULSE visual system. It keeps
the on-premises application independently installable and buildable without a
checkout of the cloud repository. Update this snapshot deliberately when the
shared visual language changes; Edge releases must not depend on cloud source
paths.
